# 更新日志

本文件记录 `@jiangdaoli/dsh-skills-manager` 的每个版本变更，随 npm 包一起发布；GitHub Release 页面同步对应说明。

## 未发布 — 0.1.3（验收中）

当前预览包：`@jiangdaoli/dsh-skills-manager@0.1.3-accept.1`（本地 tarball 安装，验收通过后发布正式版 0.1.3）

### 新增
- **每页条数自定义**：工具栏新增选择器（10 / 20 / 50 / 100 条或「一页全部」），选择持久记住（localStorage），切换后回到第 1 页。
- **分页器与列表明确分离**：分页器上方加分隔线，不再与最后的技能卡片视觉粘连。

### 修复
- **技能卡片空行**：卡片结构固定为三行（名称+状态徽章 / 描述 / 来源·路径）；描述缺失或纯空白时显示灰色「暂无描述」占位，描述中的多余空白统一折叠。此前描述为空的条目第二行整行消失或不渲染内容。

### 调整
- 列表条目间距 8px → 12px，卡片内边距加大（9/12 → 11/14），长列表阅读更松。

## 0.1.2 — 2026-08-27

### 新增
- **技能列表分页**：每页 20 条，「上一页 / 第 x/y 页 / 下一页」；搜索时自动回第 1 页，操作后页码越界自动收敛；不足一页时分页条隐藏。

### 修复
- **条目间距丢失**：样式表里的 `.dsh_skm_list` 弹性容器从未被组件套用，所有条目直接平铺紧挨在一起。现已把技能行包进该容器恢复间距。

## 0.1.1 — 2026-08-21

### 变更
- 构建链路修正：pnpm 11 显式允许 esbuild 构建脚本（`allowBuilds`），保证从 npm 干净安装可用。
- 通过 GitHub Actions OIDC 可信发布（workflow 文件名与 npm 绑定）。

## 0.1.0 — 2026-08-20

首发版本。

### 功能
- **完整技能列表**：设置面板浏览全部已安装技能（名称、描述、来源、位置、状态），支持搜索过滤。
- **停用 / 启用**：重命名 `SKILL.md` ↔ `SKILL.md.disabled`，停用即从 `/` 命令列表隐藏、文件保留。
- **删除（回收站）**：移入 `~/.dsh/.skill-trash/`，可一键恢复原位置。
- **彻底删除**：回收站条目不可恢复清除，两步确认。
- **内置技能只读**：bundled 技能只读保护。
